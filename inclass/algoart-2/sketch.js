
  var ball_x;
  var ball_y;
  var ball_width = 80;

  var max_ball_mov;

function setup() {

  createCanvas(600, 600);
  background (0);

  ball_x = width + 0.5;
  ball_y = height = 0.5;

}

function draw() {
  ellipse(ball_x, ball_y, ball_width, ball_width);

  ball_x +=


//unsuccessful

}
